import styled from "styled-components";

export const PatientWrapper = styled.span`
  font-size: 1.4rem;
  font-weight: 600;
  color: var(--white);

  background-color: var(--secondary);

  width: 14.4rem;
  height: 3.2rem;
  display: flex;
  align-items: center;
  padding: 0rem 1.6rem;
  border-radius: 2rem;
`;
