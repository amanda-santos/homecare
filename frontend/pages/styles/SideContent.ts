import styled from "styled-components";

export const SideContent = styled.div`
  display: flex;
  flex-direction: column;

  margin: 0 20rem 0 13.6rem;

  h3,
  h5 {
    color: var(--title);
    font-size: 3.2rem;
    font-weight: 400;
    margin: 3.2rem 0 0 0;
    max-width: 35.2rem;
  }

  h5 {
    font-size: 2.4rem;
  }
`;
